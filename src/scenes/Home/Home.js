import React from 'react';

import { Button } from '../../components';

const Home = () => (
  <div>
    Home
    {' '}
    <Button variant="danger">Some content</Button>
  </div>
);

export default Home;
